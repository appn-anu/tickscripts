dbrp "scalearray"."autogen"

var data = batch
  |query('select * from scalearray.autogen./.*/')
      .period(24h)
      .cron('0 0 * * *')
  |alert()
    .info(lambda: TRUE)
    .exec('python3', '/etc/kapacitor/python/writecsv.py', 'scalearray')
